# Optional: ML-based projections with PyCaret
# Install separately if you want ML features
# Compatible with Python 3.9+ (including 3.12+)
# Installing from GitHub master provides latest Python compatibility
pycaret @ git+https://github.com/pycaret/pycaret.git@master
